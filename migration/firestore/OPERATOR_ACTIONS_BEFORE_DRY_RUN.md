# Operator actions before dry-run

**BLOCKED.** No commands below have been applied. These are prerequisites, not authorization for customer migration. Run commands from the repository root. Detailed IAM scopes: [IAM analysis](PRODUCTION_ENV_IAM_ANALYSIS.md). Exact deployment/removal templates: [commands](PRODUCTION_ENV_DEPLOYMENT_COMMANDS.md). Synthetic sequence: [smoke plan](PRODUCTION_REAL_SMOKE_PLAN.md).

1. **Confirm GitHub revocation and review the executable commit.** Revoke the exposed token in GitHub Settings → Developer settings → Personal access tokens; report only confirmation/date/method. Do not paste or use it. Revocation has no undo. Separately review the final executable commit and pin `approvedPreparationCommit` in `config/production-migration.json`; do not self-approve a different HEAD. Verify with `git rev-parse HEAD` and the GO orchestrator. Removing the approval restores the block. Resources: GitHub credential and local commit approval; no project mutation for the pin.

2. **BILLING OPERATOR ACTION REQUIRED.** [Upgrade project mydesckpro to Blaze](https://console.firebase.google.com/project/mydesckpro/usage/details) using an approved billing account. Minimum: project billing assignment plus account usage permission. Verify `gcloud billing projects describe mydesckpro --format="value(billingEnabled)"` returns true. Unlink only with separate approval after checking dependent workloads; no account details belong in evidence.

3. **Enable required APIs, after billing.** Minimum permission: `serviceusage.services.enable` on `mydesckpro`. Execute:

   ```powershell
   gcloud services enable cloudfunctions.googleapis.com run.googleapis.com cloudbuild.googleapis.com artifactregistry.googleapis.com firebasestorage.googleapis.com policytroubleshooter.googleapis.com iam.googleapis.com --project=mydesckpro
   gcloud services list --enabled --project=mydesckpro --format="value(config.name)"
   ```

   Eventarc is disabled but unnecessary for the current callable-only package; do not enable it speculatively. Rollback is `gcloud services disable EXACT_NEWLY_ENABLED_API --project=mydesckpro` only after dependency review, never `--force` or disable-all. Existing Firestore/Rules/Auth APIs stay enabled.

4. **Approve separated identities and bindings.** Minimum administrator changes: create `mydesck-functions`, `mydesck-storage-migration`, `mydesck-deployer`, and `mydesck-rules-reader` individually with `gcloud iam service-accounts create ACCOUNT_ID --project=mydesckpro`. Review `node migration/firestore/tools/production-iam.mjs --mode=plan`; its exact add/remove commands cover writer, runtime, Rules reader and index deployer. Apply one reviewed binding using its approval hash. Use `gcloud iam roles create MyDesckRulesDeployer --project=mydesckpro --file=migration/firestore/release/rules-deployer-role.yaml` for the narrow Rules publisher and bind it only to the deployer. Functions deployer needs `roles/cloudfunctions.developer` and `roles/iam.serviceAccountUser` on **only** the runtime SA. Human impersonation binds only named work identities. Migrate the writer's pre-existing Auth-admin duty separately; review the pre-existing human Owner as break-glass, not routine work. Verify with `node migration/firestore/tools/inspect-production-environment.mjs` and the Troubleshooter command in the IAM analysis. Remove only exact added bindings using generated removal commands; delete newly created unused identities/custom roles only after removing their bindings.

5. **INDEX DEPLOYMENT OPERATOR ACTION REQUIRED.** Review `node migration/firestore/tools/production-index-plan.mjs`; execute only its three MISSING-index commands. Minimum: deployment identity `roles/datastore.indexAdmin`; resource: database `default`. Rerun inventory and index plan until **3/3 READY**. Cleanup removes only exact created index IDs as described by the plan; no full-config deployment of unrelated field overrides.

6. **Provision and pin private Firebase Storage; resolve database integration first.** Resource: project `mydesckpro`, no bucket currently exists. Use Firebase Console → Storage → Get started, choose approved location and locked Rules. Provisioning needs `storage.buckets.create` and Firebase Storage setup permissions on the project; object migration needs only bucket-scoped `roles/storage.objectUser`. Verify via `gcloud storage buckets list --project=mydesckpro --format="value(name)"` and environment inventory. Copy the discovered Firebase-associated name into `config/production-storage-target.json`; never guess. **STOP before candidate Storage Rules deployment:** their `(default)` Firestore lookup conflicts with the verified `default` database. Confirm supported cross-service access with Firebase support and prove it synthetically before choosing a secure candidate. No duplicate database, public objects, or weakened tenant check. Bucket rollback: delete only the newly created empty bucket after confirming no objects/generations/retention/dependencies; never recursive deletion.

7. **Approve App Check and controlled candidate deployment.** Register/verify the web app and packaged Electron attestation provider in project App Check; wire the approved provider into the production client. Minimum: App Check configuration permissions, held by a deployment operator, never runtime. No debug-token proof. Current Firestore Rules are captured deny-all; owner-allowed client smoke needs an explicitly approved candidate deployment: `firebase deploy --only firestore:rules --project mydesckpro --config migration/firestore/firebase.production.json`. Verify release/source hashes by rerunning inventory. Rollback updates the same release to the captured ruleset in `release/current-production/manifest.json`; preserve that artifact before recapture. After all capability gates, build the synthetic callable package and install its locked dependencies, then deploy only `productionReadinessSmoke` with `firebase.readiness-smoke.json`. See smoke plan for exact entrypoint and checks. Remove that exact function with `firebase functions:delete productionReadinessSmoke --region us-central1 --project mydesckpro`; no application-callable activation.

8. **Complete synthetic proof and rollback, then rerun GO.** Follow the smoke plan for Auth, Function, Firestore, one Storage object, tenant rejection, journal and exact-manifest cleanup. Record the real evidence, not emulator results. Verify cleanup and rerun `node migration/firestore/tools/production-dry-run.mjs --mode=dry-run`. Current hard gates intentionally remain blocked until reviewed evidence replaces missing proof. Stop before customer migration, write freeze or backend switch.

# Isolated real-project proof — prepared, NOT RUN

No real-project resource was created. `productionReadinessSmoke` is a separate v2 callable candidate; the existing five application callables are not deployed by its package. Its input accepts only a generated run ID. It requires Auth UID `<run>--owner`, matching server-set synthetic claims, and enforced App Check. It writes only five fixed documents in `migration-test` using transaction create preconditions, including a synthetic payment, journal and idempotency entry. A replay returns the same IDs and receipt. The emulator proves rejection, transaction/idempotency and rollback planning; it does not prove production IAM or attestation.

## Prerequisites and order

1. Confirm provider token revocation, billing, exact IAM permissions, all three indexes READY, Functions capability, pinned Firebase Storage bucket, Rules candidate/rollback readiness, and approved App Check provider for browser and packaged Electron. `assertSyntheticPreflight` refuses any missing prerequisite. No real runner is exposed while these are blocked.
2. Generate `migration-test--production-readiness-<UUID>`. Persist an empty cleanup manifest before any create request. Every UID, business, trip, payment and object ID starts with this run ID plus `--`. Reject preexisting resources: no upsert/overwrite. Auth creation must preserve the requested UID and use synthetic `example.invalid` identities; never import the ten source accounts.
3. Operator may build (`node migration/firestore/tools/build-readiness-smoke-package.mjs`) and separately deploy only `productionReadinessSmoke` using `firebase.readiness-smoke.json`. The first invocation uses the synthetic UID and a valid App Check token; the second identical invocation must return replay=true and identical IDs. Persist the successful creation receipt and Firestore updateTime for every returned document, including journal/idempotency. Tokens/passwords stay only in memory/ignored local credential storage.
4. Prove real Firebase ID token verification (`aud=mydesckpro`, matching `sub`/UID), rejected missing/other-user tokens and rejected App Check. Admin IAM success is not tenant-isolation proof. The isolated `migration-test` collection is denied to clients by candidate Rules; record that fact. Separately test owner and other-tenant client reads on **only** synthetic IDs in the actual `businesses`/`trips` schema after approved Rules deployment. The current production deny-all policy cannot pass an owner-allowed test. No customer ID or wildcard listing is allowed. Dedicated synthetic business/trip schema fixtures and all Function-generated side records must be enumerated before that separate client test; the isolated callable does not claim to prove application-schema reads.
5. Storage: pin the actual returned bucket name; upload one object containing fixed synthetic bytes into `businesses/<run>--business/signatures/<run>--object.png`. Record generation, owner run marker and SHA-256. Verify authorized SDK upload/read and equal SHA-256; anonymous and a second synthetic tenant must be denied. Server migration IAM and client Storage Rules are different tests. Do not issue a public download token or copy a signature/attachment.
6. Backend stays Supabase. The synthetic journal has beforeHash=null and an exact synthetic payment afterHash. Compare against an empty local synthetic source mirror to detect Firestore-only state. Reconcile/cleanup without any Supabase write; the payment is fixed at `17.25 ILS`. This tests pre-cutover rollback. The later customer post-cutover reverse adapter/journal integration remains a separate milestone.
7. Cleanup: record every created UID/document/object before proceeding. Require matching live migrationRunId and creation receipt, and version preconditions (Firestore updateTime, Storage generation). Auth cleanup requires live matching custom claims and recorded creation timestamp/UID, performed only after disabling the synthetic identity and ensuring no concurrent work. Delete only exact recorded resources in reverse dependency order; no collection/bucket/user wildcard deletion. If an unexpected derived ID is not supported by the reviewed cleanup guard, stop before creating it and extend/test the manifest adapter. Prove each recorded resource absent afterwards; retain hashed evidence, not credentials.

## Capability discrepancies that must remain visible

Storage candidate `rules/storage.rules` currently dereferences `/databases/(default)/documents/businesses/...`, whereas the only production database is `default`. The emulator baseline seeds `(default)`, so its PASS is insufficient evidence for this project. Official Storage documentation limits cross-service access to the default Firestore database. **BLOCKED_DATABASE_REFERENCE_MISMATCH:** operator/platform confirmation and a real synthetic integration proof are required before any Storage Rules deployment. Do not create a second database, duplicate customer tenancy data, remove the ownership check, or claim billing alone fixes it. [Storage cross-service limitations](https://firebase.google.com/docs/storage/security/rules-conditions).

The application production client currently initializes Auth/Firestore/Functions/Storage but does not initialize an App Check provider. The existing App Check plan needs an approved attestation provider and endpoint/registration, especially for packaged Electron. This is a provider/configuration dependency plus subsequent client integration; it is not solved by an environment flag. Do not use debug tokens as production proof or disable `enforceAppCheck`.

## Function classification

| Function or group | Deployment / activation classification | Bulk-write effects |
| --- | --- | --- |
| `productionReadinessSmoke` | DORMANT_SAFE_TO_DEPLOY after capability approval; claims restrict it to the one synthetic run | Callable only, no event trigger, no external effects |
| `travelAnalytics` | DORMANT_SAFE_TO_DEPLOY after capability approval | Callable, read-only |
| `saveTrip`, `recordPayment`, `recordInstallmentPayment`, `setTripState` | ENABLE_AFTER_CUTOVER; do not deploy as an unguarded dormant package | Callable only; no bulk Firestore trigger, but authorized calls can write |
| Future Storage cleanup / scheduled cleanup | MIGRATION_SUPPRESSED, absent from package | Do not enable during migration |
| WhatsApp and other notification integrations | NOTIFICATION_QUARANTINED | Absent from candidate exports |
| Staff/privilege and document-generation legacy operations | ENABLE_AFTER_CUTOVER | Absent from candidate exports |

Current callables use 2nd gen. Eventarc is inventoried but not required for these HTTP/callable-only candidates. If a trigger is added, its Eventarc/Pub/Sub/service-agent requirements and migration suppression need a new review.
